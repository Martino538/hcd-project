<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head.ejs'); %>
    <title>Martijn's API site</title>
</head>
<body>
    <main>
        <div class="container">
            <h1>Kledingkeuze</h1>
            <p>Welkom op de website van de kledingkast. Vul het onderstaande formulier in om passende kleding te vinden. 
                Nadat het fomulier is ingevuld worden de passende kledingstukken getoond.</p>
            <form action="/resultaten" method="post">
                <div class="form-group">
                    <label for="kleur">Welke kleur wil je dragen?</label>
                    <select id="kleur" name="kleur">
                        <% kleuren.forEach((kleur) => { %>
                            <option value="<%= kleur.naam %>"><%= kleur.naam %></option>
                        <% }); %>
                    </select>
                </div>

                <!-- <div class="form-group">
                    <label for="positie">Wat voor soort kledingstuk wil je zien?</label>
                    <select id="positie" name="positie">
                        <option value="bovenkleding">Bovenkleding</option>
                        <option value="onderkleding">Onderkleding</option>
                    </select>
                </div> -->

                <div class="form-group">
                    <label for="type">Welk type kledingstuk wil je dragen?</label>
                    <select id="type" name="typeKleding">
                        <% kleding.forEach((item) => { %>
                            <option value="<%= item.type %>"><%= item.type %></option>
                        <% }); %>
                    </select>
                </div>

                <input type="submit" value="Verstuur formulier">
            </form>
        </div>
    </main>
</body>
</html>